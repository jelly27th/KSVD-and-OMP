cmake_minimum_required(VERSION 3.8)

# # basic debug
# set(CMAKE_CXX_FLAGS   "-std=c++11")             # c++11
# set(CMAKE_CXX_FLAGS   "-g")                     # debug messages
# set(CMAKE_CXX_FLAGS   "-Wall")                  # open all warning settings
# # gdb debug
# SET(CMAKE_BUILD_TYPE "Debug")
# SET(CMAKE_CXX_FLAGS_DEBUG "$ENV{CXXFLAGS} -O0 -Wall -g2 -ggdb")
# SET(CMAKE_CXX_FLAGS_RELEASE "$ENV{CXXFLAGS} -O3 -Wall")

project(KSVD)

find_package(OpenCV 4.5.0 REQUIRED)
include_directories(${OpenCV_INCLUDE_DIRS})
find_package(Eigen3 REQUIRED)
include_directories(${EIGEN3_INCLUDE_DIR})

include_directories(_omp)
file(GLOB _OMP CONFIGURE_DEPENDS _omp/*.cpp)
include_directories(ksvd)   
file(GLOB KSVD CONFIGURE_DEPENDS ksvd/*.cpp)
include_directories(utils)
file(GLOB UTILS CONFIGURE_DEPENDS utils/*.cpp)
include_directories(_math)
file(GLOB _MATH CONFIGURE_DEPENDS _math/*.cpp)

add_executable(KSVD main.cpp ${_OMP} ${KSVD} ${UTILS} ${_MATH})

target_link_libraries(KSVD ${OpenCV_LIBS})